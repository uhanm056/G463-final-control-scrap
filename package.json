{
  "name": "g463-final-control-scrap",
  "version": "0.1.0",
  "private": true,
  "description": "G463 Prefix — dashboard finální kontroly, posouzení a scrapu (self-contained HTML)",
  "scripts": {
    "build": "node build.js",
    "test": "node test/parser.test.js"
  }
}
